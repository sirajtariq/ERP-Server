version: '3.8'

services:
  backend:
    build: .
    container_name: erp_backend_app
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DEBUG=True
    command: >
      sh -c "uv run python manage.py makemigrations &&
             uv run python manage.py migrate &&
             uv run python manage.py runserver 0.0.0.0:8000"